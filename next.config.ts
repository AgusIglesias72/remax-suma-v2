import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lh3.googleusercontent.com", "res.cloudinary.com", "images.unsplash.com", "plus.unsplash.com", "d1acdg20u0pmxj.cloudfront.net"],
  },
};

export default nextConfig;
